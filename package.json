{
  "name": "fbkt-pg",
  "version": "1.0.44",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server --NODE_ENV dev",
    "test": "node server --NODE_ENV test --command runAllUnitTests",
    "wipeDb": "node server --NODE_ENV buildDb --command wipeDb",
    "buildDb": "node server --NODE_ENV buildDb --command buildDb",
    "patchDb": "node server --NODE_ENV buildDb --command patchDb"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv-safe": "^4.0.3",
    "dottie": "^2.0.0",
    "fbkt": "^1.0.36",
    "ramda": "^0.22.1"
  },
  "devDependencies": {
    "chai": "^3.5.0"
  }
}
